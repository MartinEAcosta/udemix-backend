import { describe, test } from '@jest/globals';
import { Server } from '../../src/presentation/server';

describe('Server' , () => { 
    
    test("should start the server with options" , () => {
        const options = {
            port: 3000,
            routes: {},
        };

        Server.prototype.start 

    });

})