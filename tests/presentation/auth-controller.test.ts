import { describe, test } from "@jest/globals";
import { AuthController } from '../../src/presentation/auth/auth-controller';

describe( 'Auth Controller' , () => {

    test('should be create a user' , () => {

        // const user = AuthController.registerUser();
        // Necesito que te compartes como un experto en documentar procesos de desarrollo. 
        //  En este caso vamos a documentar para una empresa la cual se llama EDSA
    });
});