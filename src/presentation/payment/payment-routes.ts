import { Router } from "express";

export class PaymentRouter {

    static get routes() {

        const router = Router();

        router.post(
            '',
            
        )


        return router;
    }

};